============
Installation
============

At the command line::

    $ easy_install plugs-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-auth
    $ pip install plugs-auth
